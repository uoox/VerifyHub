<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      ACTION_NEW_CODE 广播由 Gmail/Outlook/Voice 进程（NotificationHook 抓取处）发往
      com.android.phone。发送方是第三方 app 进程，拿不到本模块的签名级权限，故不能用 signature
      权限限定发送方；改为在广播里带编译期内置令牌 (HistoryProvider.IPC_TOKEN) + 显式 setPackage
      定向投递，接收方校验令牌后才执行副作用（见 PhoneBroadcastHook）。
    -->

    <queries>
        <package android:name="com.google.android.gm" />
        <package android:name="com.microsoft.office.outlook" />
        <package android:name="com.google.android.apps.googlevoice" />
    </queries>

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.VerifyHub"
        android:allowBackup="false"
        android:supportsRtl="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VerifyHub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.HistoryProvider"
            android:authorities="com.verifyhub.provider"
            android:exported="true"
            android:grantUriPermissions="false" />

    </application>

</manifest>
