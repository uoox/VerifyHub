<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      保护 ACTION_NEW_CODE 广播（内含明文验证码 + 会触发按键注入）。
      signature 级：只有以本模块签名签署的应用（即 com.verifyhub 自己）才被授予，
      第三方应用无法伪造该广播去驱动 com.android.phone 里的注入。
      同时 uses-permission 让本 App 自身持有它，其发出的广播才能通过接收方校验。
    -->
    <permission
        android:name="com.verifyhub.permission.NEW_CODE"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.verifyhub.permission.NEW_CODE" />

    <queries>
        <package android:name="com.google.android.gm" />
        <package android:name="com.microsoft.office.outlook" />
        <package android:name="com.google.android.apps.googlevoice" />
    </queries>

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.VerifyHub"
        android:allowBackup="false"
        android:supportsRtl="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VerifyHub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.HistoryProvider"
            android:authorities="com.verifyhub.provider"
            android:exported="true"
            android:grantUriPermissions="false" />

    </application>

</manifest>
